{
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.1",
    "gh-pages": "^6.3.0",
    "vite": "^7.1.3"
  },
  "scripts": {
    "build": "vite build",
    "deploy": "gh-pages -d dist",
    "predeploy": "npm run build"
  }
}
